# Core dependencies (for inference / game running)
openai>=1.3.5
colorama>=0.4.6

# Refiner inference dependencies (for using the trained Refiner model)
torch>=2.0.0
transformers>=4.38.0
peft>=0.7.0

# Training dependencies (for reward server and GRPO training)
# fastapi>=0.104.0
# uvicorn>=0.24.0
# ms-swift  # https://github.com/modelscope/ms-swift
